django>=5.2,<5.3
djangorestframework>=3.15,<3.16
drf-spectacular>=0.28,<0.29
djangorestframework-simplejwt>=5.5,<5.6
psycopg[binary]>=3.2,<3.3
redis>=5.2,<5.3
celery>=5.4,<5.5
django-environ>=0.11,<0.12
django-cors-headers>=4.6,<4.7
django-filter>=24.3,<25.0
argon2-cffi>=23.1,<24.0
structlog>=24.4,<25.0
django-structlog>=8.1,<9.0
nh3>=0.2,<0.3
gunicorn>=23.0,<24.0
uvicorn[standard]>=0.32,<0.33
sentry-sdk>=2.18,<3.0
cryptography>=49.0,<51.0
stripe>=11.1,<12.0
pyotp>=2.9,<3.0
# Store.logo (ImageField) -- Phase F business-info step, first real
# uploaded-file field in this project (every other "image" so far is a
# preview_image_url reference, not merchant-uploaded content). Floor
# raised to 12.1 (from an initial 11.x pin) after `pip-audit` found the
# 11.3.0 that satisfied the original constraint carries ~20 known CVEs,
# all fixed by 12.1.1+ -- caught by this phase's own required
# pip-audit run, not shipped and left for later.
Pillow>=12.1,<13.0
